# Открытые вопросы

Что не подтверждено, что отложено и почему. Закрытый пункт — **удаляется**, а не помечается
сделанным. Веха из закрытого пункта — запись в `log.md`.

## Не подтверждено

- **Лимит HTML 1.5 rps — это короткая серия.** Probe подтвердил 2.0 rps sustained (30 запросов)
  и 3.0 rps burst (12), безопасным взят 1.5. Длинный Tier-1 краул (~250 команд) на 1.5 rps ещё не
  прогнан целиком — первый полный прогон покажет, держится ли чистый статус на дистанции.
- **429/Retry-After не наблюдались** — логика ретраев их уважает, но эмпирически не проверена.

## Известные болячки (задокументированы, не блокеры)

- TM 403-ит API на `market_value` (статичный UA/requests) — работает прямой fallback на ceapi.
  Если в transfermarkt-api сменят транспорт — fallback останется резервным.
- Отдельные страницы игроков 403 даже для curl_cffi (похоже на honeypot) — пропускаются и
  кэшируются, канон строится без них.

## Отложено (вне этой сессии)

- **Конвертер канона → GF SQLite** (teams/players/leagues, `base_stat`, `profile_xml`, формации).
- **Конвертер канона → Flutter `prepared_*.json`** для football_collection.
- **Генератор китов** по `template_kit.png` (color1 → торс, color2 → рукава/шорты/носки).
- `national_teams.flag` — источник флага сборных в API не найден (только эмблема).
- `competitions.logo` — логотипы лиг не достаются (null в каноне).

## Отвергнуто

- Ничего существенного за сессию.