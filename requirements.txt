requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
# curl_cffi imitates Chrome TLS fingerprint and bypasses part of Cloudflare.
# On exotic platforms where it cannot install, client.py falls back to requests.
curl_cffi>=0.7.0
jsonschema>=4.0

# tests
pytest>=8.0
pytest-timeout>=2.0